# install perf with this command
# The packages with uname -r unravel to something like linux-tools-4.15.0-24-generic
sudo apt install linux-tools-common linux-tools-generic linux-tools-`uname -r` linux-cloud-tools-`uname -r`
